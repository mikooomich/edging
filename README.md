This is a serious edging project.

## Building and running

You may use cmake if you wish to.

You may also
g++ main.cpp bitmap.cpp -o whyareyourunning && ./whyareyourunning
for simplicity.

1. Create a folder named "data". Create folders "data/input", "data/output", "data/temp". You can also use the template data folder provided
Put image(s) to process is `data/input/` The output images are stored in `data/output/`.
2. Run the program

[//]: # (You can view output_display/index.html to check the results after executing main.)

cheers.